<div class="container _module-home-hero"
        {% if post.get_field('welcome-bg_image') %}
            style="background-image: url({{ post.get_field('welcome-bg_image') }})"
        {% elseif fallback_header_image %}
        	style="background-image: url({{ TimberImage(fallback_header_image) }})"
        {% endif %}
        >
    <div class="container-inner">
    
        {% include '_modules/_module-home-welcome.twig' %}

    </div>
</div>